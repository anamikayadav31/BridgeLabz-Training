<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\FundooNotesApp.RepositoryLayer\FundooNotesApp.RepositoryLayer.csproj" />
    <ProjectReference Include="..\FundooNotesApp.ModelLayer\FundooNotesApp.ModelLayer.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- BCrypt.Net-Next: industry-standard one-way password hashing. -->
    <PackageReference Include="BCrypt.Net-Next" Version="4.0.3" />
    <!-- Lets us read appsettings.json values like the JWT secret key. -->
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" Version="8.0.0" />
    <!-- Builds and signs JWT access tokens for login. -->
    <PackageReference Include="System.IdentityModel.Tokens.Jwt" Version="7.5.1" />
    <!-- Lets us publish/consume messages on a RabbitMQ queue (used for the forget-password email flow). -->
    <PackageReference Include="RabbitMQ.Client" Version="6.8.1" />
    <!-- Redis client, used to cache short-lived password-reset tokens with an automatic expiry. -->
    <PackageReference Include="StackExchange.Redis" Version="2.7.33" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
